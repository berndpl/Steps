# Xcode / build artifacts
build/
DerivedData/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# Local, machine-specific build settings (signing team) — referenced by project.yml
Local.xcconfig

# Note: Steps.xcodeproj is generated by XcodeGen (project.yml is the source of truth),
# but it's kept in the repo so the project opens without running xcodegen first.
